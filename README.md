dotvim
======

Doesn't have the bundles in in order to avoid .git nonsense.

So after cloning do:

git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle

Then bundle install should work?
